// Scaffolding-Platzhalter, damit das Paket baut, bevor echte Typen existieren.
// Wird durch die ersten Foundational-Typen (ServerConfig, ImmichError, …) ersetzt.
// Kein Feature-Code.

enum ImmichClientModule {
    static let scaffold = true
}
