// ThemeKit — Foundation-only leaf package holding the slideshow's display/playback
// preferences (ThemeSettings) and an injectable, persistable settings store.
//
// The value types and the ThemeSettingsStore protocol land here in Phase 2 (feature
// 008). This file marks the package root; real types live in their own files.
