// ThemeKitTestSupport — shared test fakes for ThemeKit (mirrors ImmichClientTestSupport).
//
// Ships the InMemoryThemeStore fake (Phase 2) as its own product so SlideshowKitTests
// and the app-hosted tests can import it without depending on a test target.
